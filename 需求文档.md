# 子女身高预测软件 — 需求文档（v0.5）

## 1. 项目概述

### 1.1 背景
家长普遍关心孩子的成年身高。现有工具多仅使用父母身高套用简单公式，未利用祖辈、旁系亲属等家族身高信息，也未结合孩子当前生长情况。本项目提供一个**网页应用**，综合家族身高数据与孩子当前生长发育信息，输出更贴合个体的成年身高预测值、区间与生长曲线。

### 1.2 目标
- 让家长在 2 分钟内完成录入，得到直观、可信、可解释的成年身高预测。
- 家族数据越全，预测越精细；仅填父母也能给出基线结果。
- 全程纯本地计算，不上传、不存储任何个人数据。

### 1.3 非目标（本期不做）
- 不替代医学诊断，不提供治疗/干预建议。
- 不做账号体系、云端同步、历史记录。
- 不做移动 App、小程序（后续可扩展）。

---

## 2. 目标用户与使用场景

| 项目 | 内容 |
|---|---|
| 主要用户 | 关注孩子成年身高的家长 |
| 使用场景 | 家庭自测、体检后参考、为孩子运动/营养规划做参考 |
| 使用环境 | 桌面或手机浏览器，单次会话 |
| 使用频次 | 低频（孩子成长阶段偶尔复测） |
| 前置知识 | 无需医学背景，界面用通俗语言 |

---

## 3. 术语与缩写

| 术语 | 说明 |
|---|---|
| MPH | Mid-Parental Height，父母平均身高法 |
| CMH | Corrected Mid-Parental Height，校正父母平均身高法（结合孩子当前身高/年龄） |
| KR | Khamis-Roche 法，基于孩子年龄/身高/体重/父母身高的预测法 |
| 目标身高 | 由家族身高推导出的遗传身高中心值 |
| 百分位 | 相对同龄同性别参照人群的位置（P3/P10/P50/P90/P97 等） |
| SDS/Z 值 | 标准差评分，偏离参照人群均值的标准差数 |
| 月龄 | 自出生起的整月数，本项目孩子年龄统一以月龄输入与计算 |
| WHO 标准 | 世界卫生组织儿童生长标准，本项目百分位与 SDS 的参照依据 |

---

## 4. 功能需求

### 4.1 输入

#### 4.1.1 必填项
| 字段 | 类型 | 校验规则 |
|---|---|---|
| 孩子性别 | 枚举（男/女） | 必选 |
| 生长标准 | 枚举（WHO / 中国） | 下拉框选择，默认 WHO |
| 父亲身高 | 数值 cm | 100–250，整数或 1 位小数 |
| 母亲身高 | 数值 cm | 100–250 |

#### 4.1.2 选填项
| 分组 | 字段 | 说明 |
|---|---|---|
| 孩子当前信息 | 月龄、当前身高、体重 | 用于 CMH/KR 校正与逐年曲线；月龄为整数月，身高为整数或 1 位小数 |
| 祖辈 | 祖父、祖母、外祖父、外祖母身高 | 各 100–250cm |
| 旁系亲属 | 叔伯、姑、姨、舅身高 | 固定四类；可动态增删多条，每条含关系与身高 |
| 其他 | 备注 | 自由文本，仅本地展示 |

旁系亲属关系固定为四类，其性别可推导，无需用户另行填写：
- 叔伯（父系男性）、姑（父系女性）、姨（母系女性）、舅（母系男性）

#### 4.1.3 交互要求
- 父母身高为默认展开的必填区；其余分组默认折叠，标注“选填，填写可提升精度”。
- 旁系亲属支持“+ 添加一位亲属”，每条含：关系（叔伯/姑/姨/舅）、身高。
- 生长标准以下拉框选择，可在 **WHO** 与 **中国** 标准之间切换，切换后重新计算百分位、SDS 与亲属标准化基准。
- 单位仅支持公制 **cm**，不做英寸/英尺换算。
- 孩子年龄以**月龄**输入（整数月），不接受出生日期换算。
- 实时校验：越界或格式错误即时提示，不阻塞其它输入。
- 提供“一键清空”和“示例数据”按钮。

### 4.2 校验与异常处理
- 身高必须为正数且在合理范围内（cm），整数或最多 1 位小数；非数值提示“需为有效数值，请确认”，超出范围提示合理区间并请用户确认数值。
- 孩子月龄须为 0–216（0–18 岁）的整数；填写了当前身高/体重却缺月龄时提示补全。
- 亲属数据缺失不影响计算，仅影响精度等级标注。
- 重复/矛盾数据（如同一关系多条）允许保留，计算时取均值并提示。

### 4.3 预测引擎

采用**公式打底 + 数据修正**的分层策略，按可用数据自动选择精度档位。

#### 4.3.1 基线：父母平均身高法（MPH）
- 男孩：`(父高 + 母高 + 13) / 2`
- 女孩：`(父高 + 母高 − 13) / 2`
- 目标身高区间：中心值 ± 8.5cm（约 ±2SD）

#### 4.3.2 校正：CMH / KR（当提供孩子当前信息时）
- 用孩子当前身高、年龄、性别，计算其相对同龄参照人群的 SDS。
- 按骨龄/年龄校正目标身高：`校正目标身高 = 目标身高 + k × SDS`（k 为性别相关系数，需文献标定）。
- 当同时有体重且月龄在 48–210 月区间（约 4–17.5 岁），自动选用 KR 法作为基础方法（替代 CMH）；不满足该条件但有月龄与身高时，回退 CMH，否则使用 MPH 基线。

#### 4.3.3 修正：家族亲属数据加权（实验性）
- 将祖辈、旁系亲属身高换算为性别校正后的“遗传贡献值”，对 MPH 中心值做**加权修正**。
- 权重遵循“亲缘系数越大权重越高”的原则（父母 > 祖辈 = 旁系）：父母 0.5，祖辈与旁系各 0.25（祖辈与旁系同为二级亲属、亲缘系数相同，故权重一致）；权重集中配置并公开。
- 修正幅度设上限（如 ±4cm），避免少量亲属数据过度影响结果。
- 该档结果需明确标注为“含家族数据修正（实验性）”。

#### 4.3.4 精度档位与置信区间
| 档位 | 触发条件 | 区间宽度（示意） |
|---|---|---|
| 基础 | 仅父母 | ±8.5cm |
| 进阶 | 父母 + 孩子当前信息 | ±5cm |
| 增强 | 父母 + 孩子信息 + 亲属 | ±4cm |
> 具体系数与区间需在算法设计阶段用 WHO 生长标准标定，文档先定义框架。

#### 4.3.5 参照数据（可切换）
提供两套参照标准，用户在下拉框中选择，用于孩子当前身高的 SDS 与百分位、以及生长曲线绘制：

| 标准 | 覆盖范围 | 数据来源 |
|---|---|---|
| WHO | 0–228 月 | WHO Child Growth Standards（0–5 岁）+ WHO 2007 Growth Reference（5–19 岁），官方逐月 LMS |
| 中国 | 0–216 月 | 中国城市儿童生长参考（Zong & Li 2013），离散月龄节点 LMS |

- 年龄轴以**月龄**为索引（预测至 216 月）。
- 中国标准原始数据为离散节点，非节点月龄对 L/M/S 做**线性插值**；WHO 为逐月节点，无插值误差。
- **预测值与标准解耦**：MPH / Khamis-Roche / CMH 与亲属修正在 **WHO 锚点框架**内计算，输出的成年身高预测值为绝对身高（cm），**不随参照标准切换而改变**；切换标准仅重新计算孩子当前身高的 SDS、百分位与生长曲线，即"同一预测值画在不同参照人群的曲线上"。
- 在界面注明所选标准来源与版本；说明 WHO 为国际多中心参照，中国标准基于城市儿童队列。
- 结果页需标注本次计算采用的参照标准。

### 4.4 输出

1. **成年身高预测值 + 区间**：醒目展示中心值与范围，标注精度档位与所用参照标准。
2. **百分位 / 生长曲线图**：孩子当前身高在参照曲线上的位置；预测成年点标注在曲线末端。
3. **逐年预测**：以月龄为轴，从当前月龄到 216 月的预测身高曲线（无当前信息时从出生起示意）。
4. **预测依据说明**：
   - 列出使用了哪些数据、落入哪个精度档位。
   - 用通俗语言说明各亲属数据对结果的大致影响方向。
5. **导出/分享**：生成结果卡片，**同时支持导出 JPG 与 PDF**，便于保存与分享。
6. **免责声明**：明确“仅供参考，不构成医学建议”。

### 4.5 隐私与数据
- 所有计算在浏览器本地完成，**不上传、不存储**任何个人数据。
- 关闭页面即清空；不使用 Cookie/本地存储保存个人身高数据。
- 不接入第三方分析或广告脚本。
- 若后续加入匿名统计，须单独征得同意并去标识化。

---

## 5. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 首屏 < 2s；计算 < 100ms（纯前端） |
| 兼容 | 主流浏览器最近两个版本；适配手机窄屏 |
| 可用性 | 无需注册；关键字段有说明气泡与示例 |
| 可访问性 | 键盘可操作；颜色对比达标；图表有文字替代 |
| 国际化 | 先做简体中文；预留文案抽离便于多语言 |
| 可维护性 | 预测算法与 UI 解耦；算法参数集中配置并有测试 |
| 可测试性 | 核心公式与档位选择有单元测试与固定样例 |

---

## 6. 数据模型（前端内部）

```
Profile {
  child: { sex, ageMonths?, heightCm?, weightKg? }   // ageMonths: 0–216 整数
  parents: { fatherCm, motherCm }                    // cm
  grandparents?: { paternalGrandfatherCm?, paternalGrandmotherCm?,
                   maternalGrandfatherCm?, maternalGrandmotherCm? }
  relatives?: [ { relation: 'uncle_paternal' | 'aunt_paternal'
                            | 'aunt_maternal' | 'uncle_maternal', heightCm } ]
  note?: string                                      // 备注，仅本地展示
}
PredictionResult {
  pointEstimateCm: number
  intervalCm: { low, high, halfWidth }
  tier: 'basic' | 'advanced' | 'enhanced'
  method: 'MPH' | 'CMH' | 'KR' | 'family-adjusted'
  baseMethod: 'MPH' | 'CMH' | 'KR'
  reference: 'WHO' | 'CHINA'
  referenceLabel: string
  childSds?: number
  familyAdjustmentCm: number
  percentiles: [ { ageMonths, p3, p10, p50, p90, p97 } ]
  yearly: [ { ageMonths, heightCm } ]
  explanation: string[]
}
```

---

## 7. 主要页面与流程

1. **录入页**：性别 + 父母身高（必填）→ 折叠的选填区 → “开始预测”。
2. **结果页**：
   - 顶部：成年身高预测值 + 区间 + 精度档位徽章。
   - 中部：生长曲线图（含百分位与预测点）。
   - 下部：逐年预测表/图、预测依据、免责声明。
   - 操作：修改输入、导出结果卡片（JPG / PDF）、重新填写。
3. **说明页**：方法简介、公式与数据来源、常见问题。

流程：`录入 → 校验 → 选择档位 → 计算 → 展示 → 导出`。

---

## 8. 约束与假设

- 家族身高与成年身高存在相关，但预测存在个体差异，无法保证准确。
- 旁系亲属数据权重与修正模型缺少大规模训练数据，本期定位为“实验性”，需显著标注。
- 孩子当前身高受测量误差与发育节奏影响，CMH/KR 结果需以区间形式呈现。
- 早产、疾病、激素治疗等特殊人群不在模型覆盖范围，需在说明中提示。

---

## 9. 里程碑建议

| 阶段 | 交付 |
|---|---|
| M1 | 录入 + MPH 基础预测 + 结果展示 + 免责声明 |
| M2 | 孩子当前信息 → CMH/KR 校正 + 百分位曲线 |
| M3 | 亲属数据加权修正（实验性）+ 逐年预测 + 导出卡片（JPG/PDF） |
| M4 | 测试、可访问性、说明页完善 |

---

## 10. 已确认决策

| # | 决策 |
|---|---|
| 1 | 旁系亲属固定为**叔伯 / 姑 / 姨 / 舅**四类（性别由关系推导） |
| 2 | 生长曲线参照标准初始采用 **WHO**（0–5 岁标准 + 5–19 岁参考）；后经决策 #7 扩展为 WHO 与中国标准并列可选 |
| 3 | 单位仅支持公制 **cm**，不做英寸/英尺 |
| 4 | 结果卡片导出**同时兼容 JPG 与 PDF** |
| 5 | 孩子年龄统一使用**月龄**（整数，0–216），不采用出生日期，本期不做骨龄 |
| 6 | **不做**多孩子批量对比，单次仅预测一个孩子 |
| 7 | 增加**中国生长标准**，与 WHO 并列，界面用**下拉框切换** |
| 8 | 预测的成年身高为**绝对值（cm），不随参照标准切换而改变**；切换标准仅影响 SDS、百分位与生长曲线（替代 v0.3 的"人口映射"设计） |

---

## 附：方法学备注（供算法阶段参考）

- MPH 为经典、透明的基线，适合所有用户。
- CMH 通过孩子当前身高 SDS 修正遗传目标，适合有当前信息的用户。
- KR 适用于 4–17.5 岁（约 48–210 月龄），需年龄、身高、体重、父母身高，精度较高但有适用年龄限制。
- WHO 标准按天/月龄给出 LMS 参数，本项目以月龄为索引查表，SDS 由 LMS 公式计算。
- 亲属数据修正拟采用**亲缘系数加权**的透明模型，而非黑盒机器学习，以保证可解释性；若后续能获得合规数据集，再评估统计/ML 模型的增益。
